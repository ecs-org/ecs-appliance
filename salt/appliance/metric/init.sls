include:
  - .cadvisor
{#
  - .pghero
  - .node-exporter
  - .memcached-exporter
  - .postgres_exporter
  - .prometheus
#}

# monitor postgres, memcached, (redis), (both nginx), uwsgi,
# cpu-load, memory, disk-i/o, disk-free, container
